<div>Followers of <%= @user.email %></div>
<% if @followers.present? %>
  <%@followers.each do |follower| %>
    <div>
      <p> <%= follower.email %></p>
    </div>
  <% end %>
<% else %>
  <div>No Record Found.</div>
<% end %>